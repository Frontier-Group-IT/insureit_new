# Mobile preview OTA — 2026-09-04

Internal customer claim Stage 2 (`Spot Status`) now reuses the recent external Spot Status presentation while preserving the internal claim as Claims Desk-controlled/read-only.

Source implementation: PR #1252, merged to `main` as `6deb72c5ec22880abaa75daad8763c823fcec5a8`.

This note records the preview OTA publication of the merged source. No APK/AAB/native build is required for this UI-only change.

Preview OTA trigger: requested from current `main` after device verification showed the previous bundle still active.
