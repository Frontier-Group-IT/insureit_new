name: Publish Claim Intimation bulk upload preview OTA

on:
  push:
    branches:
      - fix/claim-intimation-bulk-upload-v2
    paths:
      - '.github/workflows/tmp-claim-intimation-bulk-upload-preview.yml'

permissions:
  contents: write

jobs:
  publish:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
    steps:
      - name: Check out feature branch source
        uses: actions/checkout@v4
        with:
          ref: fix/claim-intimation-bulk-upload-v2

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Install dependencies
        run: npm ci --no-audit --no-fund

      - name: Publish Expo preview OTA
        working-directory: apps/mobile-app
        run: npx eas-cli update --channel preview --environment preview --message "Enable multi-file upload on Claim Intimation" --non-interactive

      - name: Remove temporary OTA workflow
        if: success()
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          sha=$(gh api repos/${{ github.repository }}/contents/.github/workflows/tmp-claim-intimation-bulk-upload-preview.yml?ref=fix/claim-intimation-bulk-upload-v2 --jq .sha)
          gh api --method DELETE repos/${{ github.repository }}/contents/.github/workflows/tmp-claim-intimation-bulk-upload-preview.yml \
            -f message='Remove temporary Claim Intimation bulk upload preview workflow' \
            -f sha="$sha" \
            -f branch='fix/claim-intimation-bulk-upload-v2'
