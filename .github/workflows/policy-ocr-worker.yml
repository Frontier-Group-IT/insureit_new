name: Run policy OCR worker

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  process:
    name: Process bounded OCR training sample
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - name: Call protected Vercel worker
        env:
          WORKER_URL: https://portal.insureit.in/api/internal/policy-ocr/worker
          WORKER_SECRET: ${{ secrets.POLICY_OCR_WORKER_SECRET }}
          ORCHESTRATOR_ID: ${{ secrets.POLICY_OCR_ORCHESTRATOR_ID }}
        shell: bash
        run: |
          set -euo pipefail
          test -n "$WORKER_SECRET"
          test -n "$ORCHESTRATOR_ID"
          curl --fail-with-body --silent --show-error \
            --retry 2 --retry-delay 5 \
            -X POST "$WORKER_URL" \
            -H "content-type: application/json" \
            -H "x-insureit-policy-ocr-worker-secret: $WORKER_SECRET" \
            --data "{\"limit\":25,\"orchestrator_id\":\"$ORCHESTRATOR_ID\"}"
