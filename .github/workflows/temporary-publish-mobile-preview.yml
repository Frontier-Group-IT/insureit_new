name: Temporary publish mobile preview OTA

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  publish:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Check out OTA source
        uses: actions/checkout@v4
        with:
          ref: 8816aad97cde2e839980dd943e7757c0ce86c636

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Publish preview OTA
        working-directory: apps/mobile-app
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
          CI: "1"
          EAS_SKIP_AUTO_FINGERPRINT: "1"
        run: >-
          npx --yes eas-cli@18 update
          --branch preview
          --environment preview
          --message "Restore mobile preview app state"
          --non-interactive
          --json

      - name: Verify latest preview update
        working-directory: apps/mobile-app
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
          CI: "1"
        run: npx --yes eas-cli@18 update:list --branch preview --limit 1 --json --non-interactive
