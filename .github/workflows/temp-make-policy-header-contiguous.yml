name: Temporary make policy header contiguous

on:
  push:
    paths:
      - scripts/temp_make_policy_header_contiguous.py
      - .github/workflows/temp-make-policy-header-contiguous.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Apply policy header continuity patch
        run: python scripts/temp_make_policy_header_contiguous.py
      - name: Install dependencies
        run: npm ci
      - name: Typecheck web portal
        run: npm run typecheck:web
      - name: Validate patch
        run: |
          git diff --check
          grep -Fq 'sticky top-[72px] z-50 mb-4 flex gap-1' apps/web-portal/components/policy-unified-form.tsx
          ! grep -Fq 'sticky top-[72px] z-50 -mt-5 mb-4' apps/web-portal/components/policy-unified-form.tsx
      - name: Commit application change
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add apps/web-portal/components/policy-unified-form.tsx
          if git diff --cached --quiet; then
            echo "No application changes to commit"
          else
            git commit -m "Make policy header and navigator contiguous"
            git pull --rebase origin main
            git push origin main
          fi
