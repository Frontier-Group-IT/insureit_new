name: Temporary publish cumulative mobile recovery preview

on:
  push:
    branches:
      - recovery/mobile-today-cumulative
    paths:
      - '.github/workflows/tmp-publish-mobile-recovery-preview.yml'

permissions:
  contents: write

jobs:
  publish:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
    steps:
      - name: Check out cumulative recovery branch
        uses: actions/checkout@v4
        with:
          ref: recovery/mobile-today-cumulative
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Install dependencies
        run: npm ci --no-audit --no-fund

      - name: Publish cumulative recovery to Expo preview
        working-directory: apps/mobile-app
        run: |
          npx eas-cli update \
            --channel preview \
            --environment preview \
            --message "Cumulative mobile recovery: PRs #588 #593 #595 #603 #607" \
            --non-interactive

      - name: Remove temporary workflow
        if: success()
        run: |
          git config user.name "antnish1"
          git config user.email "shahdolho@gmail.com"
          git rm .github/workflows/tmp-publish-mobile-recovery-preview.yml
          git commit -m "chore: remove temporary cumulative recovery preview workflow"
          git push origin HEAD:recovery/mobile-today-cumulative
