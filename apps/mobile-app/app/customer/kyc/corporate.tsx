import { MaterialCommunityIcons } from '@expo/vector-icons';
import * as DocumentPicker from 'expo-document-picker';
import { useRouter } from 'expo-router';
import { useEffect, useState } from 'react';
import { ActivityIndicator, Modal, Pressable, ScrollView, StyleSheet, Text, TextInput, View } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';

import { BrandLogo } from '@/components/first-look';
import { ensureCustomerOnboardingForPartner, getCurrentSession, getOnboardingDocuments, saveOnboardingDraft } from '@/lib/auth';
import { supabase } from '@/lib/supabase';
import { palette } from '@/lib/theme';
import type { CustomerOnboardingApplication, CustomerOnboardingDocument, IndiaLocation, Json } from '@/lib/types';

type FleetBand = 'less_than_5' | '5_to_20' | '20_to_50' | 'more_than_50';
type PickedFile = { uri: string; name: string; mimeType: string | null; size: number | null };
type Contact = { name: string; phone: string; email: string };
const maxFileSize = 5 * 1024 * 1024;
const panPattern = /^[A-Z]{5}[0-9]{4}[A-Z]$/;
const gstPattern = /^[0-9]{2}[A-Z]{5}[0-9]{4}[A-Z][1-9A-Z]Z[0-9A-Z]$/;
const fleetOptions: { value: FleetBand; label: string }[] = [
  { value: 'less_than_5', label: 'Less than 5' }, { value: '5_to_20', label: '5 to 20' },
  { value: '20_to_50', label: '20 to 50' }, { value: 'more_than_50', label: 'More than 50' },
];

export default function CorporateKycScreen() {
  const router = useRouter();
  const [application, setApplication] = useState<CustomerOnboardingApplication | null>(null);
  const [documents, setDocuments] = useState<CustomerOnboardingDocument[]>([]);
  const [companyName, setCompanyName] = useState(''); const [companyPan, setCompanyPan] = useState(''); const [gstNumber, setGstNumber] = useState('');
  const [street, setStreet] = useState(''); const [locality, setLocality] = useState(''); const [postalCode, setPostalCode] = useState('');
  const [location, setLocation] = useState<IndiaLocation | null>(null); const [locationOptions, setLocationOptions] = useState<IndiaLocation[]>([]);
  const [fleetBand, setFleetBand] = useState<FleetBand | null>(null);
  const [ceo, setCeo] = useState<Contact>({ name: '', phone: '', email: '' });
  const [admin, setAdmin] = useState<Contact>({ name: '', phone: '', email: '' });
  const [spoc, setSpoc] = useState<Contact>({ name: '', phone: '', email: '' });
  const [panFile, setPanFile] = useState<PickedFile | null>(null); const [gstFile, setGstFile] = useState<PickedFile | null>(null);
  const [loading, setLoading] = useState(true); const [submitting, setSubmitting] = useState(false); const [error, setError] = useState(''); const [successVisible, setSuccessVisible] = useState(false);

  useEffect(() => { let active = true; (async () => {
    try {
      const session = await getCurrentSession(); if (!session?.user) return router.replace('/login');
      const nextApplication = await ensureCustomerOnboardingForPartner(session.user, 'corporate');
      if (['submitted','under_review'].includes(nextApplication.status)) return router.replace('/customer/home');
      if (nextApplication.partner_type !== 'corporate') {
        if (active) setError('Your Corporate KYC could not be opened. Go back and choose the partner type again.');
        return;
      }
      const nextDocuments = await getOnboardingDocuments(nextApplication.id); if (!active) return;
      setApplication(nextApplication); setDocuments(nextDocuments);
      const draft = asDraft(nextApplication.draft_data);
      setCompanyName(textDraft(draft,'company_name')); setCompanyPan(textDraft(draft,'company_pan')); setGstNumber(textDraft(draft,'gst_number'));
      setStreet(textDraft(draft,'address_street')); setLocality(textDraft(draft,'address_locality')); setPostalCode(textDraft(draft,'postal_code'));
      const savedFleet = textDraft(draft,'fleet_size_band'); if (fleetOptions.some((item) => item.value === savedFleet)) setFleetBand(savedFleet as FleetBand);
      const locationId = textDraft(draft,'india_location_id'); if (locationId) { const result = await supabase.from('india_locations').select('*').eq('id',locationId).maybeSingle(); if (result.data) setLocation(result.data); }
      const contacts = await supabase.from('customer_onboarding_contacts').select('contact_role,full_name,phone,email').eq('application_id',nextApplication.id);
      for (const contact of contacts.data ?? []) { const value = { name: contact.full_name ?? '', phone: String(contact.phone ?? '').replace(/^\+91/,''), email: contact.email ?? '' }; if (contact.contact_role === 'ceo_head') setCeo(value); if (contact.contact_role === 'admin_head') setAdmin(value); if (contact.contact_role === 'dedicated_spoc') setSpoc(value); }
      const reviewNotes = textDraft(draft,'review_notes'); if (nextApplication.status === 'changes_requested' && reviewNotes) setError(`Please update your application: ${reviewNotes}`);
    } catch { if (active) setError('We could not open Corporate KYC. Please try again.'); } finally { if (active) setLoading(false); }
  })(); return () => { active = false; }; }, [router]);

  useEffect(() => { if (postalCode.length !== 6 || location?.pincode === postalCode) return setLocationOptions([]); const timer = setTimeout(async () => { const result = await supabase.from('india_locations').select('*').eq('pincode',postalCode).order('city_name').limit(12); if (result.error) return setError('PIN lookup is unavailable.'); const options = result.data ?? []; setLocationOptions(options); if (options.length === 1) { setLocation(options[0]); setLocationOptions([]); } },300); return () => clearTimeout(timer); }, [postalCode, location?.pincode]);

  async function chooseFile(kind: 'company_pan_copy'|'gst_copy') { const result = await DocumentPicker.getDocumentAsync({ type:['application/pdf','image/jpeg','image/png'], copyToCacheDirectory:true }); if (result.canceled || !result.assets[0]) return; const asset = result.assets[0]; if (asset.size && asset.size > maxFileSize) return setError('Document must be 5 MB or smaller.'); const file = { uri:asset.uri,name:asset.name,mimeType:asset.mimeType ?? null,size:asset.size ?? null }; kind === 'company_pan_copy' ? setPanFile(file) : setGstFile(file); }
  function validate() { if (companyName.trim().length < 2) return 'Enter company name.'; if (!panPattern.test(companyPan)) return 'Enter a valid company PAN.'; if (gstNumber && !gstPattern.test(gstNumber)) return 'Enter a valid GSTIN.'; if (!street.trim() || !location) return 'Complete the company address and select city.'; if (!fleetBand) return 'Select fleet size.'; for (const [label,contact] of [['CEO / Head',ceo],['Admin Head',admin],['Dedicated SPOC',spoc]] as const) { if (contact.name.trim().length < 2) return `Enter ${label} name.`; if (!/^\d{10}$/.test(contact.phone)) return `Enter valid ${label} mobile.`; if (contact.email && !/^\S+@\S+\.\S+$/.test(contact.email)) return `Enter valid ${label} email.`; } if (new Set([ceo.phone,admin.phone,spoc.phone]).size !== 3) return 'Each contact must use a different mobile number.'; const existing = new Set(documents.filter((item) => item.verification_status !== 'rejected').map((item) => item.document_type)); if (!panFile && !existing.has('company_pan_copy' as never)) return 'Attach company PAN copy.'; if (gstNumber && !gstFile && !existing.has('gst_copy')) return 'Attach GST certificate.'; return ''; }

  async function submit() { if (!application || submitting) return; const issue = validate(); if (issue) return setError(issue); if (!location || !fleetBand) return; setSubmitting(true); setError(''); try {
    const draft: Json = { company_name:companyName.trim(), company_pan:companyPan, gst_number:gstNumber || null, address_street:street.trim(), address_locality:locality.trim() || null, india_location_id:location.id, city:location.city_name, state:location.state_name, postal_code:location.pincode, fleet_size_band:fleetBand };
    await saveOnboardingDraft(application.id,draft,3);
    if (panFile) await uploadDocument(application.id,'company_pan_copy',panFile); if (gstFile) await uploadDocument(application.id,'gst_copy',gstFile);
    const { error: submitError } = await supabase.rpc('submit_corporate_onboarding_application',{ p_application_id:application.id,p_company_name:companyName.trim(),p_company_pan:companyPan,p_gst_number:gstNumber || null,p_address_street:street.trim(),p_address_locality:locality.trim() || null,p_india_location_id:location.id,p_city:location.city_name,p_state:location.state_name,p_postal_code:location.pincode,p_fleet_size_band:fleetBand,p_ceo_name:ceo.name.trim(),p_ceo_phone:ceo.phone,p_ceo_email:ceo.email || null,p_admin_name:admin.name.trim(),p_admin_phone:admin.phone,p_admin_email:admin.email || null,p_spoc_name:spoc.name.trim(),p_spoc_phone:spoc.phone,p_spoc_email:spoc.email || null });
    if (submitError) throw submitError; setSuccessVisible(true);
  } catch (nextError) { setError(nextError instanceof Error ? nextError.message : 'Corporate KYC could not be submitted.'); } finally { setSubmitting(false); } }

  if (loading) return <SafeAreaView style={styles.safe}><View style={styles.loading}><ActivityIndicator size="large" color="#0A43A3"/><Text>Preparing Corporate KYC</Text></View></SafeAreaView>;
  return <SafeAreaView style={styles.safe} edges={['top','bottom']}><View style={styles.header}><Pressable onPress={() => router.replace('/customer/kyc/partner-type')} style={styles.back}><MaterialCommunityIcons name="chevron-left" size={27} color={palette.navy}/></Pressable><BrandLogo width={145}/><View style={{width:42}}/></View><ScrollView contentContainerStyle={styles.content} keyboardShouldPersistTaps="handled"><Text style={styles.title}>Corporate KYC</Text>{error ? <View style={styles.error}><Text style={styles.errorText}>{error}</Text></View> : null}<Section title="Company details"><Field label="Company name" value={companyName} onChangeText={setCompanyName}/><Field label="Company PAN" value={companyPan} onChangeText={(v)=>setCompanyPan(v.replace(/[^a-z0-9]/gi,'').toUpperCase().slice(0,10))}/><Field label="GSTIN optional" value={gstNumber} onChangeText={(v)=>setGstNumber(v.replace(/[^a-z0-9]/gi,'').toUpperCase().slice(0,15))}/><FileButton label="Company PAN copy" file={panFile?.name} onPress={()=>chooseFile('company_pan_copy')}/>{gstNumber ? <FileButton label="GST certificate" file={gstFile?.name} onPress={()=>chooseFile('gst_copy')}/> : null}</Section><Section title="Company address"><Field label="Street" value={street} onChangeText={setStreet}/><Field label="Locality" value={locality} onChangeText={setLocality}/><Field label="PIN code" value={postalCode} onChangeText={(v)=>{setPostalCode(v.replace(/\D/g,'').slice(0,6));setLocation(null);}} keyboardType="number-pad"/>{locationOptions.map((item)=><Pressable key={item.id} onPress={()=>{setLocation(item);setPostalCode(item.pincode);setLocationOptions([]);}} style={styles.location}><Text style={styles.locationTitle}>{item.city_name}</Text><Text style={styles.locationText}>{item.state_name} · {item.pincode}</Text></Pressable>)}{location ? <Text style={styles.selected}>{location.city_name}, {location.state_name}</Text> : null}</Section><Section title="Fleet size"><View style={styles.options}>{fleetOptions.map((item)=><Pressable key={item.value} onPress={()=>setFleetBand(item.value)} style={[styles.option,fleetBand===item.value&&styles.optionActive]}><Text style={fleetBand===item.value&&styles.optionTextActive}>{item.label}</Text></Pressable>)}</View></Section><ContactSection title="CEO / Head" value={ceo} onChange={setCeo}/><ContactSection title="Admin Head" value={admin} onChange={setAdmin}/><ContactSection title="Dedicated SPOC" value={spoc} onChange={setSpoc}/></ScrollView><View style={styles.footer}><Pressable disabled={submitting} onPress={submit} style={styles.submit}>{submitting?<ActivityIndicator color="#fff"/>:<Text style={styles.submitText}>Submit Corporate KYC</Text>}</Pressable></View><Modal visible={successVisible} transparent animationType="fade"><View style={styles.modalBackdrop}><View style={styles.modal}><Text style={styles.modalTitle}>KYC submitted</Text><Text style={styles.modalText}>The portal team can now review the company and prepare all three logins.</Text><Pressable onPress={()=>router.replace('/customer/home')} style={styles.modalButton}><Text style={styles.submitText}>Return to dashboard</Text></Pressable></View></View></Modal></SafeAreaView>;
}

async function uploadDocument(applicationId:string,type:string,file:PickedFile){ const session=await getCurrentSession(); if(!session?.user) throw new Error('Sign in again.'); const extension=file.name.split('.').pop()?.toLowerCase()||'bin'; const path=`${applicationId}/${type}/${Date.now()}-${Math.random().toString(36).slice(2)}.${extension}`; const response=await fetch(file.uri); const bytes=await response.arrayBuffer(); const upload=await supabase.storage.from('customer-documents').upload(path,bytes,{contentType:file.mimeType??'application/octet-stream',upsert:false}); if(upload.error) throw upload.error; const record=await supabase.from('customer_onboarding_documents').upsert({application_id:applicationId,document_type:type,file_name:file.name,storage_bucket:'customer-documents',storage_path:path,mime_type:file.mimeType,file_size:file.size,verification_status:'pending',uploaded_by:session.user.id},{onConflict:'application_id,document_type'}); if(record.error){await supabase.storage.from('customer-documents').remove([path]); throw record.error;} }
function Section({title,children}:{title:string;children:React.ReactNode}){return <View style={styles.card}><Text style={styles.sectionTitle}>{title}</Text>{children}</View>}
function Field(props:React.ComponentProps<typeof TextInput>&{label:string}){const{label,...rest}=props;return <View style={styles.field}><Text style={styles.label}>{label}</Text><TextInput {...rest} placeholderTextColor="#9AA7B8" style={styles.input}/></View>}
function FileButton({label,file,onPress}:{label:string;file?:string;onPress:()=>void}){return <Pressable onPress={onPress} style={styles.file}><MaterialCommunityIcons name="paperclip" size={18} color="#0A43A3"/><Text style={styles.fileText}>{file||label}</Text></Pressable>}
function ContactSection({title,value,onChange}:{title:string;value:Contact;onChange:(value:Contact)=>void}){return <Section title={`${title} login`}><Field label="Full name" value={value.name} onChangeText={(name)=>onChange({...value,name})}/><Field label="Mobile number" value={value.phone} onChangeText={(phone)=>onChange({...value,phone:phone.replace(/\D/g,'').slice(0,10)})} keyboardType="number-pad"/><Field label="Email optional" value={value.email} onChangeText={(email)=>onChange({...value,email})} keyboardType="email-address" autoCapitalize="none"/></Section>}
function asDraft(value:Json|null):Record<string,Json|undefined>{return value&&typeof value==='object'&&!Array.isArray(value)?value as Record<string,Json|undefined>:{};} function textDraft(draft:Record<string,Json|undefined>,key:string){const value=draft[key];return typeof value==='string'?value:'';}
const styles=StyleSheet.create({safe:{flex:1,backgroundColor:'#F6F8FC'},header:{height:66,paddingHorizontal:14,flexDirection:'row',alignItems:'center',justifyContent:'space-between',backgroundColor:'#fff',borderBottomWidth:1,borderBottomColor:'#E2E8F0'},back:{width:42,height:42,borderRadius:12,borderWidth:1,borderColor:'#D8E2EE',alignItems:'center',justifyContent:'center'},content:{padding:16,paddingBottom:30,gap:14},title:{color:palette.navy,fontSize:22,fontWeight:'800'},error:{backgroundColor:'#FEF3F2',borderRadius:11,padding:11},errorText:{color:'#B42318'},card:{backgroundColor:'#fff',borderWidth:1,borderColor:'#D9E2ED',borderRadius:16,padding:15},sectionTitle:{color:palette.navy,fontSize:15,fontWeight:'800',marginBottom:12},field:{marginBottom:12},label:{fontSize:12,fontWeight:'700',color:palette.navy,marginBottom:6},input:{minHeight:50,borderWidth:1,borderColor:'#D4DDE8',borderRadius:11,paddingHorizontal:12,color:'#17202F'},file:{minHeight:48,borderWidth:1,borderStyle:'dashed',borderColor:'#BFD0E4',borderRadius:11,flexDirection:'row',alignItems:'center',gap:8,paddingHorizontal:12,marginBottom:10},fileText:{color:'#0A43A3',fontWeight:'700'},options:{gap:8},option:{minHeight:44,borderWidth:1,borderColor:'#D4DDE8',borderRadius:10,justifyContent:'center',paddingHorizontal:12},optionActive:{backgroundColor:'#0A43A3',borderColor:'#0A43A3'},optionTextActive:{color:'#fff',fontWeight:'800'},location:{padding:10,borderWidth:1,borderColor:'#D8E2EE',borderRadius:10,marginBottom:7},locationTitle:{fontWeight:'700',color:palette.navy},locationText:{fontSize:11,color:'#64748B'},selected:{color:'#16845B',fontWeight:'700'},footer:{padding:12,backgroundColor:'#fff',borderTopWidth:1,borderTopColor:'#E2E8F0'},submit:{minHeight:54,borderRadius:12,backgroundColor:'#0A3B8F',alignItems:'center',justifyContent:'center'},submitText:{color:'#fff',fontWeight:'800'},loading:{flex:1,alignItems:'center',justifyContent:'center',gap:12},modalBackdrop:{flex:1,backgroundColor:'rgba(15,23,42,.45)',alignItems:'center',justifyContent:'center',padding:24},modal:{backgroundColor:'#fff',borderRadius:20,padding:24,width:'100%',maxWidth:390,alignItems:'center'},modalTitle:{fontSize:20,fontWeight:'800',color:palette.navy},modalText:{marginTop:8,textAlign:'center',color:'#59687A',lineHeight:20},modalButton:{marginTop:20,minHeight:48,borderRadius:12,backgroundColor:'#0A3B8F',paddingHorizontal:20,justifyContent:'center'}});
