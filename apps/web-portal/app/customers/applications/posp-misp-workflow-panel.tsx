import { FormSubmitButton } from "@/components/form-submit-button";
import { createSupabaseAdminClient } from "@/lib/supabase-admin";
import { completePospMispDocumentStage,decidePospMispPartnerRoute,markPospMispReadyForOnboarding,movePospMispToIib,retryPospMispPanVerification } from "./posp-misp-workflow-actions";

export type PospMispWorkflowProfile={workflow_stage:"pre_iib"|"iib_processing"|"training"|"completed";partner_type:"posp"|"misp";pan_number:string|null;dp_pan_number:string|null;iib_remarks:string|null;iib_uploaded:boolean;iib_uploaded_at:string|null;training_login_id:string|null;training_credentials_shared_flag:boolean;training_start_date:string|null;training_end_date:string|null;training_status:string|null;training_certificate_number:string|null;exam_status:string|null;onboarding_date:string|null};
type PanJob={status:"pending"|"queued"|"checking"|"matched"|"not_found"|"invalid"|"failed"|"manual_review";result_message:string|null;attempt_count:number;last_error:string|null;checked_by_device:string|null};
type RouteState={requested_account_type:"posp"|"misp"|null;final_account_type:"posp"|"misp"|"partner"|null;partner_decision:"not_applicable"|"pending"|"convert_to_partner"|"do_not_proceed"};

export async function PospMispWorkflowPanel({applicationId,profile}:{applicationId:string;profile:PospMispWorkflowProfile}){
 const admin=createSupabaseAdminClient();const [{data:job},{data:route},{count:documentCount}]=await Promise.all([admin.from("pan_verification_jobs").select("status,result_message,attempt_count,last_error,checked_by_device").eq("application_id",applicationId).maybeSingle<PanJob>(),admin.from("posp_misp_onboarding_profiles").select("requested_account_type,final_account_type,partner_decision").eq("application_id",applicationId).maybeSingle<RouteState>(),admin.from("intermediary_onboarding_documents").select("id",{count:"exact",head:true}).eq("application_id",applicationId)]);
 const stage=profile.workflow_stage;const partnerDecision=route?.partner_decision??"not_applicable";const partnerRoute=job?.status==="matched"&&partnerDecision==="convert_to_partner";const canContinue=job?.status==="not_found"||partnerRoute;const finalType=route?.final_account_type??route?.requested_account_type??profile.partner_type;const iibPan=profile.partner_type==="misp"?profile.dp_pan_number:profile.pan_number;
 return <section className="overflow-hidden rounded-2xl border border-[#DCE5EF] bg-white shadow-sm"><header className="border-b border-[#E2E8F0] bg-[#F8FAFC] px-4 py-3"><div className="flex items-center justify-between gap-3"><div><p className="text-[9px] font-semibold uppercase tracking-[.1em] text-[#64748B]">Onboarding progress</p><h2 className="mt-1 text-sm font-semibold text-[#0F172A]">{stageTitle(stage)}</h2></div><span className="rounded-full border border-[#C7D2FE] bg-[#EEF2FF] px-2.5 py-1 text-[9px] font-semibold uppercase text-[#4338CA]">{finalType}</span></div><div className="mt-3 grid grid-cols-3 gap-2"><Step active={stage==="pre_iib"} done={stage!=="pre_iib"} label="Primary & IIB"/><Step active={stage==="iib_processing"} done={stage==="training"||stage==="completed"} label="Documents"/><Step active={stage==="training"||stage==="completed"} done={stage==="completed"} label="Review"/></div></header><div className="border-b border-[#E2E8F0] px-4 py-4"><div className="flex items-start justify-between gap-3"><div><p className="text-[9px] font-semibold uppercase tracking-[.08em] text-[#64748B]">Automatic IIB status</p><p className="mt-1 text-[11px] font-semibold text-[#0F172A]">{maskPan(job?.status?iibPan:null)}</p></div><StatusBadge status={job?.status??"pending"}/></div><p className="mt-2 text-[10px] text-[#64748B]">{jobMessage(job)}</p>{job?.checked_by_device?<p className="mt-1 text-[8.5px] text-[#94A3B8]">Checked by {job.checked_by_device} · Attempt {job.attempt_count}</p>:null}</div>{job?.status==="matched"&&partnerDecision==="pending"?<div className="border-b border-amber-200 bg-amber-50 px-4 py-4"><p className="text-[11px] font-semibold text-amber-900">This PAN is already registered in IIB.</p><p className="mt-1 text-[9.5px] text-amber-800">Continue as a Business Associate or close the application.</p><div className="mt-3 grid grid-cols-2 gap-2"><form action={decidePospMispPartnerRoute}><input type="hidden" name="application_id" value={applicationId}/><input type="hidden" name="partner_decision" value="convert_to_partner"/><FormSubmitButton label="Create as Partner" pendingLabel="Saving" className="w-full rounded-lg bg-violet-600 px-3 py-2 text-[10px] font-semibold text-white"/></form><form action={decidePospMispPartnerRoute}><input type="hidden" name="application_id" value={applicationId}/><input type="hidden" name="partner_decision" value="do_not_proceed"/><FormSubmitButton label="Do Not Proceed" pendingLabel="Closing" className="w-full rounded-lg border border-red-200 bg-white px-3 py-2 text-[10px] font-semibold text-red-700"/></form></div></div>:null}{partnerRoute?<div className="border-b border-violet-200 bg-violet-50 px-4 py-3"><p className="text-[10.5px] font-semibold text-violet-800">Partner route selected</p></div>:null}<div className="p-4">{stage==="pre_iib"?<>{job?.status==="failed"||job?.status==="invalid"?<form action={retryPospMispPanVerification}><input type="hidden" name="application_id" value={applicationId}/><FormSubmitButton label="Retry PAN check" pendingLabel="Re-queuing" className="w-full rounded-lg bg-[#071D49] px-4 py-2.5 text-[10.5px] font-semibold text-white"/></form>:null}<form action={movePospMispToIib}><input type="hidden" name="application_id" value={applicationId}/><FormSubmitButton label="Continue to Documents" pendingLabel="Opening documents" disabled={!canContinue} className="mt-2 w-full rounded-lg bg-gradient-to-r from-[#6759ff] to-[#4f8df6] px-4 py-2.5 text-[10.5px] font-semibold text-white disabled:opacity-40"/></form></>:stage==="iib_processing"?<form action={completePospMispDocumentStage}><input type="hidden" name="application_id" value={applicationId}/><p className="mb-3 text-[9.5px] text-[#64748B]">{documentCount??0} document(s) attached.</p><FormSubmitButton label="Documents Complete" pendingLabel="Completing" disabled={!documentCount} className="w-full rounded-lg bg-gradient-to-r from-[#6759ff] to-[#4f8df6] px-4 py-2.5 text-[10.5px] font-semibold text-white disabled:opacity-40"/></form>:stage==="training"?<form action={markPospMispReadyForOnboarding}><input type="hidden" name="application_id" value={applicationId}/><FormSubmitButton label="Mark Ready for Onboarding" pendingLabel="Completing" className="w-full rounded-lg bg-emerald-600 px-4 py-2.5 text-[10.5px] font-semibold text-white"/></form>:<p className="text-center text-[10.5px] font-semibold text-emerald-700">Onboarding workflow completed.</p>}</div></section>;
}
function Step({label,active,done}:{label:string;active:boolean;done:boolean}){return <div className={`rounded-lg border px-2 py-2 text-center text-[8.5px] font-semibold ${done?"border-emerald-200 bg-emerald-50 text-emerald-700":active?"border-[#C7D2FE] bg-[#EEF2FF] text-[#4338CA]":"border-[#E2E8F0] bg-white text-[#94A3B8]"}`}>{done?"✓ ":""}{label}</div>}
function StatusBadge({status}:{status:PanJob["status"]}){const map:Record<string,[string,string]>={pending:["Waiting","bg-slate-100 text-slate-700"],queued:["Queued","bg-blue-50 text-blue-700"],checking:["Checking","bg-blue-50 text-blue-700"],matched:["Existing IIB record","bg-amber-50 text-amber-700"],not_found:["IIB cleared","bg-emerald-50 text-emerald-700"],invalid:["Invalid PAN","bg-red-50 text-red-700"],failed:["Check failed","bg-red-50 text-red-700"],manual_review:["Manual review","bg-violet-50 text-violet-700"]};const[label,style]=map[status]??map.pending;return <span className={`rounded-full px-2.5 py-1 text-[8.5px] font-semibold ${style}`}>{label}</span>}
function jobMessage(job:PanJob|null|undefined){if(!job)return"Waiting for the extension to collect this PAN.";return job.result_message??job.last_error??({pending:"Waiting for the extension.",queued:"Queued for checking.",checking:"The extension is checking this PAN in IIB POS.",matched:"Matching Record Found In DataBase",not_found:"No Data Found In POS System",invalid:"The PAN could not be checked.",failed:"The last check failed.",manual_review:"Manual review is required."}[job.status])}
function stageTitle(stage:PospMispWorkflowProfile["workflow_stage"]){return stage==="pre_iib"?"Primary information & IIB check":stage==="iib_processing"?"Document upload":stage==="training"?"Final review":"Completed"}
function maskPan(value:string|null){return value&&value.length===10?`${value.slice(0,2)}****${value.slice(-3)}`:"PAN pending"}
