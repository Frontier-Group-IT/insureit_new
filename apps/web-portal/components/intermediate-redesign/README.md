/**
 * Redesigned Intermediary Register page components.
 *
 * This folder contains a complete visual and interaction redesign of the
 * intermediary register pages (POSP, MISP, Partner, Overview).
 *
 * Key improvements over the original:
 * - Base font size bumped from 10.5px to 12px (table cells) and 13px (values)
 * - Standardized color tokens (grays, status colors) from a single source
 * - Color-coded status badges with semantic meaning (error=gray, warning=amber, success=green, info=indigo)
 * - Sticky table headers with backdrop blur
 * - Batch selection via checkboxes with bulk actions dropdown
 * - Wider action column with clearly separated buttons
 * - Consistent 12px border-radius system
 * - Zebra row striping via subtle border-top
 * - Responsive fallback: tables become stacked cards on sm screens
 *
 * All components are self-contained and do NOT modify any existing files.
 */
