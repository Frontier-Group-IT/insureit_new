{
  "purpose": "Deploy Policy Portfolio responsive filter overlap fix.",
  "deploy_requested_at": "2026-08-20T18:01:00+05:30",
  "requested_by": "explicit user deploy request",
  "source": "Deploy current main using the protected GitHub Actions production deployment workflow.",
  "source_commit": "7e93cbcba43186731eb5b3bdb056a141939be8f1",
  "release_scope": "Deploy PR #481, which fixes Policy Portfolio filter overlap at 100% zoom by reducing the custom date filter width, allowing the filter group to wrap when needed, and keeping the status tabs in a separate non-overlapping flex area. Existing date placeholders, dd-mm-yyyy display, native picker behavior, ISO date state, filtering logic, backend logic, database schema, Supabase, permissions, and policy workflows remain unchanged.",
  "verification": "PR #481 Verify web portal run 32369200431 passed successfully, including regressions, typecheck, lint, and the Next.js production build. This release has no Supabase migration. The protected production workflow must verify current main again before invoking the Vercel production deploy hook.",
  "instructions": "Update this file only after explicit deployment approval."
}
